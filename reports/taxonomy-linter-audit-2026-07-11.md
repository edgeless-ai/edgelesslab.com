# Taxonomy linter audit — 2026-07-11

- `scripts/cron/taxonomy-triage.py: DEPRECATED` — duplicates policy that already lives in `CLAUDE.md`, allowing silent drift. Derive the runtime rules from the Canonical Locations table and retain the old list only as a warned fallback.
- `/backlog/tasks/`, `vault/backlog/tasks/`, `/.backlog/` — present in `CLAUDE.md` but absent from `DEPRECATED`. Parse and scan these task paths (normalizing the generic `vault/` prefix to the configured vault root).
- `*-backup/` and `.claude/skills/*/templates/` — glob rules in `CLAUDE.md` are absent from `DEPRECATED`; backup folders are only partly covered by a separate workspace-root heuristic. Support parsed glob rules directly in deprecated-path scanning.
- `chroma_data/`, `chroma_db/`, and `.chroma` — deprecated Chroma locations in `CLAUDE.md` are absent from `DEPRECATED`; `.chroma` is incorrectly protected by `PROTECTED_RUNTIME`. Parse them from the table; separately retire the protection after consumers are migrated.
- `.paperclip*`, `.hive*`, and `.backroom*` at workspace root — forbidden by both `CLAUDE.md` and `DIRECTORY-POLICY.md`, but the scanner exempts its known filenames through `PROTECTED_RUNTIME`. The parsed rules should report them in `--check`; cron exemptions remain unchanged only where explicitly required by existing behavior.
- `ad hoc inbox/queue folders`, `vault root`, `docs root`, and `new work in legacy trees` — policy rules are prose rather than machine-identifiable paths, so the linter cannot enforce them reliably. Replace these descriptions with explicit paths or globs in a future documentation change.
- `CANONICAL_NUMBERED` prefixes `14`–`18` — missing despite `claude-vault/_system/TAXONOMY.md` defining `14-Knowledge-Bases`, `15-Products`, `16-Projects`, `17-Websites`, and `18-Evals`. Derive numbered-prefix rules from the vault taxonomy (or update the map pending that refactor) so collisions on these prefixes are detectable.
- `DIRECTORY-POLICY.md: Numbers 12 and 14 are reserved/unused` — `14` conflicts with the vault taxonomy's active `14-Knowledge-Bases`. Update the policy to reserve only `12` and reflect the current allocations.
- `DIRECTORY-POLICY.md: .semantic-*` — runtime-state policy includes this prefix, while the `CLAUDE.md` deprecated cell and scanner cover only Paperclip/Hive/Backroom prefixes. Add `.semantic*` to the authoritative table so the derived linter can enforce it.
- Paperclip stamping — the default cron path still targets retired `http://127.0.0.1:3100`. Keep it isolated for compatibility as requested, and use the new `--check` branch as the pure, read-only preflight path with no report writes or API calls.
